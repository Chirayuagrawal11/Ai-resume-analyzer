CREATE DATABASE resume_analyzer;
-- Tables are created automatically by Hibernate (spring.jpa.hibernate.ddl-auto=update).
-- Run this file in pgAdmin Query Tool if the database does not already exist.
